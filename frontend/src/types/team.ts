export type TeamRequest = {
    name: string;
    description: string;
    user_id: number;
};

export type Team = {
    name: string;
    description: string;
    user_id: number;
    id: number;
};
